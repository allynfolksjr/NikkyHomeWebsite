<% if posts.present? %>
  <% posts.first(3).each do |post| %>
    <h5>
      <i class="fa-brands fa-wordpress" aria-hidden="true"></i>
      <%= link_to post.title, post.url, target: '_blank' %>
      <small>Posted <%= time_ago_in_words post.posted_at %> ago</small>
    </h5>
  <% end %>
<% end %>
